@model tuvantuyensinhsv.v2.Models.InternalMessage

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>InternalMessage</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.From)

        @Html.HiddenFor(model => model.To)

        <div class="form-group">
            @Html.LabelFor(model => model.Mesage, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Mesage)
                @Html.ValidationMessageFor(model => model.Mesage)
            </div>
        </div>

        @Html.HiddenFor(model => model.DateCreate)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
